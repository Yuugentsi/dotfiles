# 󰉋 dotfiles
### 󰉋 config
`aria2` `fish` `gallery-dl` `hypr` `kitty` `mpv` `rofi` `swayimg` `swaync` `waybar` `yt-dlp` `zathura`

```
bash <(curl -sL https://raw.githubusercontent.com/Yuugentsi/dotfile/main/dotfiles.sh)
```

### 󰉋 config only (Arch/Termux)
```
bash <(curl -sL https://raw.githubusercontent.com/Yuugentsi/dotfile/main/config.sh)
```
